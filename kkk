<!DOCTYPE html>
<html lang="th">
<head>
  <link rel="manifest" href="manifest.json">

  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>เกมของฉัน — หน้าเริ่มต้น</title>
  <style>
    :root{
      --bg1:#0f2027;
      --bg2:#203a43;
      --accent:#ffd166;
      --muted:#cbd5e1;
    }
    *{box-sizing:border-box}
    html,body{height:100%}

    body {
      margin: 0;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: url("images/bg-game.jpg") center/cover no-repeat fixed;
      color: var(--muted);
      display: grid;
      place-items: center;
      overflow: hidden; 
    }

    .hero{
      width:min(920px,92vw);
      height:min(600px,86vh);
      background: rgba(0,0,0,0.45);
      border-radius:18px;
      box-shadow: 0 10px 40px rgba(244, 244, 247, 0.6);
      position:relative;
      overflow:hidden;
      display:grid;
      grid-template-rows: 1fr auto;
      align-items:center;
      padding:36px;
      gap:12px;
      border: 1px solid rgba(255,255,255,0.03);
      backdrop-filter: blur(8px);
    }

    .title-wrap{text-align:center;margin-top:16px;}
    h1{
      margin:0 0 8px;
      font-size:clamp(28px,4vw,44px);
      line-height:1;
      color:white;
      text-shadow: 0 6px 30px rgba(255, 255, 255, 0.6);
    }
    p.lead{margin:0;color:rgba(255,255,255,0.8);font-size:16px;opacity:0.9;}

    .stage{
      display:flex;
      align-items:center;
      justify-content:center;
      gap:20px;
      padding:20px;
    }

    .start-btn{
      --size:180px;
      width:var(--size);
      height:var(--size);
      border-radius:50%;
      border:none;
      outline:none;
      display:inline-grid;
      place-items:center;
      background: radial-gradient(circle at 30% 30%, #ffffff, #ffd166 15%, #ffb703 50%);
      box-shadow: 0 8px 30px rgb(0, 0, 0), inset 0 -6px 18px rgb(245, 243, 243);
      font-weight:800;
      font-size:60px;
      cursor:pointer;
      transition: transform 200ms cubic-bezier(.2,.9,.2,1), box-shadow 200ms;
      user-select:none;
      color: white; /* ✅ ตัวอักษรสีขาว */
      text-shadow: 0 2px 6px rgba(0,0,0,0.3); /* เงาให้เด่น */
    }
    .start-btn:active{ transform:scale(.98) }
    .start-btn:focus{ box-shadow:0 0 0 6px rgba(255,177,3,0.15); }

    .controls{
      display:flex;
      justify-content:center;
      gap:12px;
      padding:12px 6px 6px;
    }
    .ghost{
      background:transparent;
      border:1px solid rgba(255,255,255,0.06);
      padding:8px 12px;
      border-radius:10px;
      color:var(--muted);
      cursor:pointer;
      font-size:14px;
    }
  </style>
</head>
<body>
  <main class="hero">
    <section class="title-wrap">
      <h1>ยินดีต้อนรับสู่เกมของคุณ</h1>
      <p class="lead">เตรียมตัว แล้วกดปุ่มเริ่ม — สนุกไปด้วยกัน!</p>
    </section>

    <section class="stage">
      <button class="start-btn"  id="startBtn">เริ่ม</button>
    </section>

    <div class="controls">
      <button class="ghost" id="howBtn">วิธีเล่น</button>
      <button class="ghost" id="settingsBtn">ตั้งค่า</button>
    </div>
  </main>

  <script>
  const startBtn = document.getElementById('startBtn');
  const howBtn = document.getElementById('howBtn');
  const settingsBtn = document.getElementById('settingsBtn');

  startBtn.addEventListener('click', ()=>{
    startBtn.animate(
      [
        { transform: 'scale(1)' },
        { transform: 'scale(1.06)' },
        { transform: 'scale(1)' }
      ],
      { duration: 420, easing: 'ease-out' }
    );

    startBtn.disabled = true;
    startBtn.textContent = 'กำลังโหลด...';

    // 🔹 เมื่อโหลดเสร็จให้ไปยัง two.html
    setTimeout(()=>{
      window.location.href = 'two.html';
    }, 900);
  });

  howBtn.addEventListener('click', ()=>{
    alert('วิธีเล่น:\n1) กดเริ่ม\n2) ทำตามคำสั่งในเกม\n3) สนุก!');
  });

  settingsBtn.addEventListener('click', ()=>{
    alert('ตั้งค่า (ตัวอย่าง):\n- เสียง: เปิด/ปิด\n- ความละเอียด: อัตโนมัติ');
  });
</script>

</body>
</html>


if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('service-worker.js');
}

.start-btn{
  --size:180px;
  width:var(--size);
  height:var(--size);
  border-radius:50%;
  border:none;
  outline:none;
  display:inline-grid;
  place-items:center;
  background: white; /* เปลี่ยนเป็นสีขาวตรงนี้ */
  color: #333;       /* ใส่สีตัวอักษรให้เด่นบนพื้นขาว */
  box-shadow: 0 8px 30px rgba(0,0,0,0.2), inset 0 -6px 18px rgba(0,0,0,0.1);
  font-weight:800;
  font-size:60px;
  cursor:pointer;
  transition: transform 200ms cubic-bezier(.2,.9,.2,1), box-shadow 200ms;
  user-select:none;
}

.start-btn:active{
  transform:scale(.98);
}

.start-btn:focus{
  box-shadow:0 0 0 6px rgba(255,177,3,0.15);
}

<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Select game level</title>
  <style>
    * { box-sizing: border-box; }
    html, body { height: 100%; }

    body {
      margin: 0;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: url("images/bg-game.jpg") center/cover no-repeat fixed;
      color: #333;
      display: grid;
      place-items: center;
      overflow: hidden;
    }

    .container {
      background: rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      border-radius: 20px;
      padding: 40px 60px;
      text-align: center;
      color: #000;
      max-width: 400px;
    }

    h1 {
      margin-bottom: 10px;
      font-size: 2em;
    }

    p {
      margin-bottom: 30px;
      font-size: 1.1em;
    }

    .btn-level {
      display: block;
      width: 100%;
      margin: 10px 0;
      padding: 12px;
      font-size: 1.1em;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      transition: 0.3s;
      color: white;
    }

    .easy {
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    }

    .medium {
      background: linear-gradient(135deg, #a1c4fd, #c2e9fb);
    }

    .hard {
      background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
    }

    .btn-level:hover {
      transform: scale(1.05);
      opacity: 0.9;
    }

    /* 🔹 ปุ่มมุมขวาล่าง */
    .back-btn {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      border-radius: 20px;
      border: none;
      background: rgba(100, 100, 100, 0.5);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      color: white;
      font-size: 26px;
      cursor: pointer;
      transition: 0.3s;
    }

    .back-btn:hover {
      transform: scale(1.1);
      background: rgba(80, 80, 80, 0.6);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Select game level</h1>
    <p>Please select the difficulty level you want.</p>
    <button class="btn-level easy" onclick="selectLevel('easy')">easy</button>
    <button class="btn-level medium" onclick="selectLevel('medium')">medium</button>
    <button class="btn-level hard" onclick="selectLevel('hard')">hard</button>
  </div>

  <!-- 🔸 ปุ่มกลับ (มุมขวาล่าง) -->
  <button class="back-btn" onclick="goBack()">&#8249;</button>

  <script>
    function selectLevel(level) {
      alert("คุณเลือกระดับ: " + level);
      // ตัวอย่าง: ไปหน้าเกมจริง
      // window.location.href = "game.html?level=" + level;
    }

    function goBack() {
      window.location.href = "two.html";
    }
  </script>
</body>
</html>

<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>เลือกระดับเกม</title>
  <style>
    * { box-sizing: border-box; }
    html, body { height: 100%; }

    body {
      margin: 0;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: url("images/bg-game.jpg") center/cover no-repeat fixed;
      color: #333;
      display: grid;
      place-items: center;
      overflow: hidden;
    }

    .container {
      background: rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      backdrop-filter: blur(15px);
      -webkit-backdrop-filter: blur(15px);
      border-radius: 20px;
      padding: 40px 60px;
      text-align: center;
      color: #000;
      max-width: 400px;
    }

    h1 {
      margin-bottom: 10px;
      font-size: 2em;
    }

    p {
      margin-bottom: 30px;
      font-size: 1.1em;
    }

    .btn-level {
      display: block;
      width: 100%;
      margin: 10px 0;
      padding: 12px;
      font-size: 1.1em;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      transition: 0.3s;
      color: white;
    }

    .easy {
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    }

    .medium {
      background: linear-gradient(135deg, #a1c4fd, #c2e9fb);
    }

    .hard {
      background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
    }

    .btn-level:hover {
      transform: scale(1.05);
      opacity: 0.9;
    }

    /* 🔹 ปุ่มมุมขวาล่าง */
    .back-btn {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      border-radius: 20px;
      border: none;
      background: rgba(100, 100, 100, 0.5);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      color: white;
      font-size: 26px;
      cursor: pointer;
      transition: 0.3s;
    }

    .back-btn:hover {
      transform: scale(1.1);
      background: rgba(80, 80, 80, 0.6);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>เลือกระดับเกม</h1>
    <p>กรุณาเลือกระดับความยากที่คุณต้องการ</p>
    <button class="btn-level easy" onclick="selectLevel('ง่าย')">ง่าย</button>
    <button class="btn-level medium" onclick="selectLevel('ปานกลาง')">ปานกลาง</button>
    <button class="btn-level hard" onclick="selectLevel('ยาก')">ยาก</button>
  </div>

  <!-- 🔸 ปุ่มกลับ (มุมขวาล่าง) -->
  <button class="back-btn" onclick="goBack()">&#8249;</button>

  <script>
    function selectLevel(level) {
      alert("คุณเลือกระดับ: " + level);
      // ตัวอย่าง: ไปหน้าเกมจริง
      // window.location.href = "game.html?level=" + level;
    }

    function goBack() {
      window.location.href = "two.html";
    }
  </script>
</body>
</html>

<!doctype html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>เลือกภาษา — Example</title>
  <style>
    :root{
      --bg: linear-gradient(135deg,#000000,#000000);
      --accent:#000000;
      --muted:#000000;
    }
    *{box-sizing:border-box}
    html,body{height:100%}

    body{
      margin: 0;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: url("images/bg-game.jpg") center/cover no-repeat fixed;
      color: #333;
      display: grid;
      place-items: center;
      overflow: hidden; 
    }

    .container{
      width:min(920px,94%);
      padding:32px;
      border-radius:14px;
      background: rgba(128,128,128,0.3);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      box-shadow:0 8px 30px rgba(0,0,0,0.4);
      text-align:center;
    }

    h1{
      margin:0 0 8px;
      font-size:clamp(20px,3.5vw,32px);
      color:#000;
    }
    p{
      margin:0 0 24px;
      color:#222;
    }

    .lang-row{
      display:flex;
      gap:18px;
      justify-content:center;
      margin:18px 0 28px;
      flex-wrap:wrap;
    }

    .lang-btn{
      border:2px solid rgba(255,255,255,0.1);
      background:rgba(255,255,255,0.2);
      color:#000;
      padding:14px 26px;
      font-size:16px;
      border-radius:999px;
      cursor:pointer;
      transition: transform .15s, background .2s;
    }
    .lang-btn:hover{
      transform:translateY(-3px);
      background:rgba(255,255,255,0.35);
    }
    .lang-btn.active{
      background:linear-gradient(90deg,#ffd166,#ff7b7b);
      color:#102027;
      border-color:transparent;
    }

    .next-btn{
      margin-top:10px;
      padding:12px 36px;
      font-size:18px;
      background:linear-gradient(90deg,#ffd166,#ff7b7b);
      color:#102027;
      border:none;
      border-radius:999px;
      cursor:pointer;
      transition:transform .15s;
    }
    .next-btn:hover{transform:translateY(-3px)}

    .back-btn{
      position:fixed;
      right:18px;
      bottom:18px;
      width:56px;
      height:56px;
      border-radius:12px;
      border:none;
      background:rgba(0,0,0,0.5);
      color:#fff;
      box-shadow:0 8px 24px rgba(2,6,23,0.6);
      cursor:pointer;
      transition:transform .12s;
    }
    .back-btn:active{transform:translateY(2px)}
  </style>
</head>
<body>
  <div class="container">
    <h1 data-i18n="title">ยินดีต้อนรับ</h1>
    <p data-i18n="subtitle">เลือกภาษาที่คุณต้องการใช้ในหน้าเว็บนี้</p>

    <div class="lang-row">
      <button class="lang-btn active" id="th-btn" data-lang="th">ไทย</button>
      <button class="lang-btn" id="en-btn" data-lang="en">English</button>
    </div>

    <button class="next-btn" id="next-btn" data-i18n="next">ถัดไป</button>
  </div>

  <button class="back-btn" id="back" title="ย้อนกลับ">
    <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
      <path d="M15 18L9 12L15 6" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </button>

  <script>
    // คำแปลสองภาษา
    const i18n = {
      th: { title:"ยินดีต้อนรับ", subtitle:"เลือกภาษาที่คุณต้องการใช้ในหน้าเว็บนี้", next:"ถัดไป" },
      en: { title:"Welcome", subtitle:"Choose the language you'd like to use on this page", next:"Next" }
    };

    // ฟังก์ชันเปลี่ยนภาษา
    function setLanguage(lang){
      document.querySelectorAll('.lang-btn').forEach(btn=>{
        const active = btn.dataset.lang === lang;
        btn.classList.toggle('active', active);
      });
      document.querySelectorAll('[data-i18n]').forEach(el=>{
        const key = el.getAttribute('data-i18n');
        if(i18n[lang][key]) el.textContent = i18n[lang][key];
      });
      document.documentElement.lang = lang;
      localStorage.setItem('preferredLang', lang);
    }

    // โหลดภาษาที่เคยเลือกไว้ หรือเริ่มต้นเป็นไทย
    const defaultLang = localStorage.getItem('preferredLang') || 'th';
    setLanguage(defaultLang);

    // ปุ่มเลือกภาษา
    document.getElementById('th-btn').onclick = ()=> setLanguage('th');
    document.getElementById('en-btn').onclick = ()=> setLanguage('en');

    // 🔹 ปุ่มย้อนกลับไปยัง firt.html
    document.getElementById('back').onclick = ()=>{
      window.location.href = 'firt.html';
    };

    // 🔹 ปุ่มถัดไป
    document.getElementById('next-btn').onclick = ()=>{
      const lang = document.documentElement.lang;
      if(lang === 'th'){
        window.location.href = 'threeTH.html';
      } else if(lang === 'en'){
        window.location.href = 'threeEN.html';
      } else {
        alert('กรุณาเลือกภาษา');
      }
    };
  </script>
</body>
</html>

:root{
  --bg1:#0f2027;
  --bg2:#203a43;
  --accent:#ffd166;
  --muted:#cbd5e1;
}

*{box-sizing:border-box}
html,body{height:100%}

/* พื้นหลัง GIF */
body {
  margin: 0;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  background: url("images/bg-game.gif") center/cover no-repeat fixed;
  color: var(--muted);
  display: grid;
  place-items: center;
  overflow: hidden; 
}

.hero{
  width:min(920px,92vw);
  height:min(600px,86vh);
  background: rgba(0,0,0,0.45);
  border-radius:18px;
  box-shadow: 0 10px 40px rgba(244, 244, 247, 0.6);
  position:relative;
  overflow:hidden;
  display:grid;
  grid-template-rows: 1fr auto;
  align-items:center;
  padding:36px;
  gap:12px;
  border: 1px solid rgba(255,255,255,0.03);
  backdrop-filter: blur(8px);
}

.title-wrap{text-align:center;margin-top:16px;}
h1{
  margin:0 0 8px;
  font-size:clamp(28px,4vw,44px);
  line-height:1;
  color:white;
  text-shadow: 0 6px 30px rgba(255, 255, 255, 0.6);
}
p.lead{margin:0;color:rgba(255,255,255,0.8);font-size:16px;opacity:0.9;}

.stage{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:20px;
  padding:20px;
}

.start-btn{
  --size:180px;
  width:var(--size);
  height:var(--size);
  border-radius:50%;
  border:none;
  outline:none;
  display:inline-grid;
  place-items:center;
  background: white; /* สีขาว */
  color: #333;       /* ตัวอักษรสีเข้ม */
  box-shadow: 0 8px 30px rgba(0,0,0,0.2), inset 0 -6px 18px rgba(0,0,0,0.1);
  font-weight:800;
  font-size:60px;
  cursor:pointer;
  transition: transform 200ms cubic-bezier(.2,.9,.2,1), box-shadow 200ms;
  user-select:none;
}
.start-btn:active{ transform:scale(.98); }
.start-btn:focus{ box-shadow:0 0 0 6px rgba(255,177,3,0.15); }

/* hover effect */
.start-btn:hover{
  transform: scale(1.05);
  box-shadow: 0 12px 40px rgba(0,0,0,0.25), inset 0 -6px 18px rgba(0,0,0,0.1);
}

.floaty{
  position:absolute;
  border-radius:14px;
  filter: blur(20px);
  opacity:0.55;
  transform:translate3d(0,0,0);
  animation: floaty 10s ease-in-out infinite;
}
.floaty.x1{width:260px;height:180px;background:linear-gradient(90deg,#7dd3fc,#60a5fa);left:-40px;top:140px;animation-duration:12s}
.floaty.x2{width:160px;height:160px;background:linear-gradient(90deg,#f0abfc,#f97316);right:-40px;bottom:60px;animation-duration:14s}
@keyframes floaty{0%{transform:translateY(0) rotate(0deg)}50%{transform:translateY(-30px) rotate(6deg)}100%{transform:translateY(0) rotate(0deg)}}

.controls{
  display:flex;
  justify-content:center;
  gap:12px;
  padding:12px 6px 6px;
}
.ghost{
  background:transparent;
  border:1px solid rgba(255,255,255,0.06);
  padding:8px 12px;
  border-radius:10px;
  color:var(--muted);
  cursor:pointer;
  font-size:14px;
}

@media (max-width:520px){
  .start-btn{--size:140px;font-size:18px}
  .hero{padding:20px}
}
